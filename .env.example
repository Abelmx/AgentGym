# OpenAI-compatible API configuration (DO NOT put secrets in this file)
#
# Usage:
#   cp .env.example .env
#   # edit .env and set OPENAI_API_KEY
#
# The runner reads these variables (or you can pass CLI args):
# - model id: MODEL_NAME or OPENAI_MODEL (or CLI: --model)
# - base url: BASE_URL or OPENAI_BASE_URL (or CLI: --base-url)
# - api key: OPENAI_API_KEY (or CLI: --api-key)
# - temperature: OPENAI_TEMPERATURE (or CLI: --temperature)
# - max output tokens: OPENAI_MAX_OUTPUT_TOKENS (or CLI: --max-output-tokens)

# Required for non-dry-run
OPENAI_API_KEY=

# Optional (defaults to https://api.openai.com)
BASE_URL=

# Optional (defaults shown below)
OPENAI_TEMPERATURE=0
OPENAI_MAX_OUTPUT_TOKENS=4096

# Required for non-dry-run (pick one)
MODEL_NAME=
# OPENAI_MODEL=

# Optional alias for BASE_URL
# OPENAI_BASE_URL=

